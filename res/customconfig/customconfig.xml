<settingsRoot>
<settingsTab name="CPU Control">

	<settingsPane description="You can change max and min frequencies of your device here. 1.26 GHz is minimum limit of max frequency as below this frequency your device may suffer from high performance degradation. 1.26 GHz is also maximum limit of min frequency as above this frequency your device may not cool down. (Default: Max 2.26 GHz, Min 300 MHz)" name="CPU Frequency control">

		<seekBar description="This slider sets maximum frequency of your device." name="Max CPU Frequency" action="genericfreq /sys/devices/system/cpu/cpu0/cpufreq/scaling_max_freq" unit="kHz" min="1267200" reversed="false" step="76800" max="2496000"/>
		<seekBar description="This slider sets minimum frequency of your device." name="Min CPU Frequency" action="genericfreq /sys/devices/system/cpu/cpu0/cpufreq/scaling_min_freq" unit="kHz" min="300000" reversed="false" step="76800" max="1267200"/>

	</settingsPane>

	<settingsPane description="You can change governor of your device here. Governor decides current frequency of your device accordingly." name="Governor control">
		<spinner description="This will set the active CPU governor." name="Default CPU Governor" action="generic /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor">
			<spinnerItem name="AbyssplugV2" value="abyssplugv2"/>
			<spinnerItem name="Conservative" value="conservative"/>
			<spinnerItem name="Intellidemand" value="intellidemand"/>
			<spinnerItem name="Interactive" value="interactive"/>
			<spinnerItem name="Ondemand" value="ondemand"/>
			<spinnerItem name="Performance" value="performance"/>
			<spinnerItem name="Powersave" value="powersave"/>
			<spinnerItem name="SmartassH3" value="smartassH3"/>
			<spinnerItem name="Userspace" value="userspace"/>
		</spinner>
	</settingsPane>

	<settingsPane description="You can change CPU IO scheduler of your device here." name="CPU IO Scheduler control">
		<spinner description="This will set the default IO scheduler for internal, system, data and external sdcard." name="Default IO Scheduler" action="generic /sys/block/mmcblk0/queue/scheduler">
			<spinnerItem name="BFQ" value="bfq"/>
			<spinnerItem name="CFQ" value="cfq"/>
			<spinnerItem name="Deadline" value="deadline"/>
			<spinnerItem name="FIOPS" value="fiops"/>
			<spinnerItem name="ROW" value="row"/>
			<spinnerItem name="SIO" value="sio"/>
			<spinnerItem name="VR" value="vr"/>
			<spinnerItem name="ZEN" value="zen"/>
		</spinner>
	</settingsPane>

	<settingsPane description="Chaos kernel uses SIMPLE governor by default for GPU. You can fine tune following parameters of SIMPLE governor according to your requirement. Default settings are balanced for power and performance." name="GPU Control">
		<seekBar description="This adjusts the number of times the governor skips ramp down requests. Higher == better performance (more battery  drain) Default 6." name="Laziness" action="generic /sys/module/msm_kgsl_core/parameters/simple_laziness" unit=" " min="0" reversed="false" step="1" max="10"/>
		<seekBar description="This adjusts the threshold to ramp up or down GPU frequencies. Lower == better performance (more battery drain) Default 5." name="Ramp Threshold" action="generic /sys/module/msm_kgsl_core/parameters/simple_ramp_threshold" unit=" " min="0" reversed="false" step="1" max="10"/>
	</settingsPane>

</settingsTab>

<settingsTab name="Memory Control">
	<settingsPane description="OOM(Out of memory) is undesired state where no additional memory resources can be allocated. You can tune behavior of OOM Killer below." name="OOM Killer">

		<checkbox description="Enable AUTO OOM control using below profiles, disable if you want to set your own OOM tuning via APP or Script." name="Auto OOM" action="generic01 /sys/module/lowmemorykiller/parameters/auto_oom" label="AUTO OOM CONTROL"/>

		<spinner description="Here you can set the level of low mem management by kernel lowmemkiller function. More aggressive, equals more free RAM but more killed apps." name="OOM Level Screen ON" action="oom_config_screen_on">
			<spinnerItem name="Low" value="low"/> 
			<spinnerItem name="Medium" value="medium"/> 
			<spinnerItem name="Default" value="default"/> 
			<spinnerItem name="Aggressive" value="aggressive"/> 
			<spinnerItem name="Exterminate" value="exterminate"/> 
		</spinner>

		<spinner description="Here you can set the level of low mem management by kernel lowmemkiller function. More aggressive equals more free RAM but more killed apps." name="OOM Level Screen OFF" action="oom_config_screen_off">
			<spinnerItem name="Low" value="low"/> 
			<spinnerItem name="Medium" value="medium"/> 
			<spinnerItem name="Default" value="default"/> 
			<spinnerItem name="Aggressive" value="aggressive"/> 
			<spinnerItem name="Exterminate" value="exterminate"/> 
		</spinner>

	</settingsPane>

	<settingsPane description="Settings for Dirty ratio and Dirty background ratio." name="VM">

		<seekBar description="Dirty real time memory buffer, more you set, more memory will be used for write buffer for real time operations. Buffer is released on need to free RAM (Default: 20%)" name="Dirty ratio" action="generic /proc/sys/vm/dirty_ratio" unit="%" min="5" reversed="false" step="5" max="90"/>

		<seekBar description="Dirty background memory buffer, more you set, more memory will be used for write buffer for background operations. Buffer is released on need to free RAM (Default: 5%)" name="Dirty Background ratio" action="generic /proc/sys/vm/dirty_background_ratio" unit="%" min="5" reversed="false" step="5" max="80"/>

	</settingsPane>

	<settingsPane description="UKSM is new technique for KSM." name="UKSM">

		<checkbox description="Enable/Disable 'Ultra KSM Control'. This will merge some memory but consumes more CPU and battery. You will need to reboot your device for changes to take effect." name="Ultra KSM Control" action="generic_cortex /tmp/uksm_control" label="Ultra KSM Control"/>
		<button description="Apply Ultra KSM changes on-the-fly." name=" " action="push-actions/uksmchange" label="Apply UKSM changes"/>
		<button description="Ultra KSM state check. Memory samepage merging is not quick process. So, stats may show 0 MB initially." name="Ultra KSM STATS" action="push-actions/uksmstats" label="Ultra KSM Stats"/>

	<button description="Clean RAM cache. This will release all cached and buffered RAM" name="CLEAN RAM CACHE" action="push-actions/clean_ram_cache" label="Clean RAM Cache"/>
		

	</settingsPane>
</settingsTab>

<settingsTab name="Wake Control">

	<settingsPane description="DoubleTap2Wake Function" name="DT2W">

		<spinner description="This option enables the doubletap2wake (AKA KnockKnock) feature (may cause more battery drain when in pocket)" name="DT2W" action="generic /sys/android_touch/doubletap2wake">
			<spinnerItem name="On" value="1"/> 
			<spinnerItem name="Off" value="0"/> 
		</spinner>

	</settingsPane>

	<settingsPane description="Sweep2Wake Function" name="S2W">

		<spinner description="This option enables the sweep2wake feature (may cause more battery drain when in pocket)" name="S2W" action="generic /sys/android_touch/sweep2wake">
			<spinnerItem name="On" value="1"/> 
			<spinnerItem name="Off" value="0"/> 
		</spinner>

		<seekBar description="This slider sets start position of S2W. Default 400." name="Start Position" action="generic /sys/kernel/s2w_parameters/s2w_start_posn" unit="" min="0" reversed="false" step="10" max="1280"/>

		<seekBar description="This slider sets start position of S2W. Default 700." name="Mid Position" action="generic /sys/kernel/s2w_parameters/s2w_mid_posn" unit="" min="0" reversed="false" step="10" max="1280"/>

		<seekBar description="This slider sets start position of S2W. Default 830." name="End Position" action="generic /sys/kernel/s2w_parameters/s2w_end_posn" unit="" min="0" reversed="false" step="10" max="1280"/>

		<seekBar description="This slider sets threshold of S2W. Default 250." name="Threshold" action="generic /sys/kernel/s2w_parameters/s2w_threshold" unit="" min="0" reversed="false" step="50" max="1000"/>

		<spinner description="This option selects orientation of S2W. Please note you will need adjust co-ordinates accordingly. Default Horizontal." name="Orientation" action="generic /sys/kernel/s2w_parameters/s2w_swap_coord">
			<spinnerItem name="Horizontal" value="0"/> 
			<spinnerItem name="Vertical" value="1"/> 
		</spinner>

	</settingsPane>
</settingsTab>

<settingsTab name="Miscellaneous">
	<settingsPane description="This section controls miscellaneous tweaks of the device." name="Misc Tweaks">

		<spinner description="Enable/Disable Dynamic File Sync. While screen is on, file sync is temporarily deferred, when screen is off, a flush is called to synchronize all outstanding writes. Please note: There may be loss of slight data if device reboots abruptly when Dynamic Fsync is enabled." name="Dynamic FSync" action="generic /sys/kernel/dyn_fsync/Dyn_fsync_active">
			<spinnerItem name="Enable" value="1"/> 
			<spinnerItem name="Disable" value="0"/> 
		</spinner>

		<spinner description="Frandom is a Linux kernel random number generator which is 10-50 times faster than what you get from Linux built-in /dev/urandom. In case you face issue with wifi tethering or bluetooth connectivity you may try disabling this option and reboot. (Reboot required)" name="FRandom" action="generic_cortex /tmp/frandom_module">
			<spinnerItem name="Enable" value="on"/> 
			<spinnerItem name="Disable" value="off"/> 
		</spinner>

		<spinner description="This option allows some cheap USB chargers with > 500ma to be recognized as an AC charger in order to charge at a faster rate." name="Fast Charge" action="generic /sys/kernel/fast_charge/force_fast_charge">
			<spinnerItem name="Enable" value="1"/> 
			<spinnerItem name="Disable" value="0"/> 
		</spinner>

		<spinner description="Backlight dimmer allows you to adjust the LCD screen brightness lower than is possible with stock. Be careful, you can lower the brightness so much you can barely see it." name="Backlight Dimmer" action="generic /sys/module/lm3630_bl/parameters/backlight_dimmer">
			<spinnerItem name="Enable" value="Y"/> 
			<spinnerItem name="Disable" value="N"/> 
		</spinner>

	</settingsPane>

	<settingsPane description="WIFI Settings" name="WIFI">

		<button description="Clean WIFI DHCP IP Lease. Push the button to activate!" name="Clean WIFI Lease" action="push-actions/fix_wifi" label="Clean WIFI Lease"/>

	</settingsPane>

	<settingsPane description="Here you can find GPS and NTP settings and options." name="GPS - NTP">

		<spinner description="GPS-Time-Zone for faster GPS lock! Use your region!" name="GPS Region" action="gpsregion">
			<spinnerItem name="No GPS Zone changes (default)" value="No_GPS_Zone_changes"/> 
			<spinnerItem name="Set Asia" value="Asia"/> 
			<spinnerItem name="Set Europe" value="Europe"/> 
			<spinnerItem name="Set Oceania" value="Oceania"/> 
			<spinnerItem name="Set North America" value="North-America"/> 
			<spinnerItem name="Set South America" value="South-America"/> 
			<spinnerItem name="Set Africa" value="Africa"/> 
			<spinnerItem name="Restore original ROM gps.conf" value="Original_ROM_GPS_Restored"/> 
		</spinner>

		<button description="Click to see your current GPS ZONE" name="GPS ZONE" action="push-actions/gpszonecheck" label="GPS Zone Check"/>

		<button description="Update time via NTP server" name="UPDATE TIME" action="push-actions/time_update" label="Update Time"/>

	</settingsPane>

</settingsTab>
<settingsTab name="Modules">

	<settingsPane description="Here, you can load kernel modules on boot that will allow some extra functions." name="Kernel Modules">

		<checkbox description="Enable/Disable Exfat module load on boot." name="EXFAT Module" action="generic_cortex /tmp/exfat_module" label="EXFAT Module"/>

		<checkbox description="Enable or Disable EDS Module, (Encrypted Data Store) lets you store your files in the encrypted container to prevent unauthorized access to sensitive information. You will need app from play store (EDS, Paid or Lite)" name="EDS Module" action="generic_cortex /tmp/eds_module" label="EDS Module"/>

		<button description="This displays the currently loaded modules." name="Show Loaded Modules" action="push-actions/show_modules" label="Show Loaded Modules"/>

		<button description="This button tries to force-load selected modules in the memory without requiring a reboot. This may not help, because most modules requires a reboot. This button is primarily for testing purposes." name="Force-Load Modules" action="push-actions/load_modules" label="Force-Load Modules"/>

	</settingsPane>
</settingsTab>
</settingsRoot>
